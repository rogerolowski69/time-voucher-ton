# Copy to .env and fill in after deploy (just deploy-testnet)
PUBLIC_MINTER_ADDRESS=
PUBLIC_NETWORK=testnet
PUBLIC_GAS_BUFFER_TON=0.3
# Issuer TON wallet address (owner) that receives redeemed TIME
PUBLIC_REDEEM_ADDRESS=
PUBLIC_CAL_COM_URL=https://cal.com/your-link
PUBLIC_ISSUER_EMAIL=you@example.com
PUBLIC_TONCENTER_API_KEY=
PUBLIC_TELEGRAM_BOT_USERNAME=your_bot

# FastAPI — never expose in frontend
TELEGRAM_BOT_TOKEN=
PORT=8787
PUBLIC_URL=https://your-app.up.railway.app
DATA_DIR=/data
ADMIN_API_TOKEN=change-me

# Query logs locally:
# curl -s -H "Authorization: Bearer $ADMIN_API_TOKEN" http://localhost:8787/api/admin/events
